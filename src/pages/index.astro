---
import List from '../components/list.astro'
import { getAllChannelPosts } from '../lib/telegram'

const channel = await getAllChannelPosts(Astro)

export const prerender = false
---

<List channel={channel} before={false} after={false} />
